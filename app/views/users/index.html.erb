<p style="color: green"><%= notice %></p>

<h1>Users that are registered</h1>

<div id="users">

  <% @users.each do |user| %>
    <%= render user %>
    <p>
      <%= link_to user.name, user %>
      
    </p>
  <% end %>
</div>

<%= link_to "New user", new_user_path %>
